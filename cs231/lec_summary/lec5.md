# Lecture 5 Image Classification with CNNs

## classification
predict 

feature representation
신경망에 입력할 값을 직접 선택, 원시 픽셀이 아닌 특징 추출하고 이미지의 픽셀 값을 
ex) 색상 히스토그램, HoG

하지만 이렇게 특징을 추출하는 것을 수동으로 하기에는 어렵다 그래서 ConvNet 가 인간보다 더 나은 방식을 소개할 것이다.
그래도 신경망을 설계해야 한다. 행렬의 크기나 연산자의 순서

CNN history
convolution, pooling operatior로 특징을 추출한다 & fully-connected layer로 구성하고 처음부터 끝까지 손실을 최소화하는 경사하강법을 통해서 최적화한다.
이걸 2010년대까지 하다가 alexnet이 나타났다. 2012-2020에 CNN의 전성기, transformer가 등장했다. 

Covolution Layer
템플릿 매칭
우리는 기존에 필터를 학습하고 이를 적용하는 것을 진행하였다 이제는 필터를 템플릿을 입력과 동일한 형태가 아니랄 입력의작은 부분집합만 보게 한다. 필터는 채널은 항상 동일해야 한다. 그리고 이 필터를 순회하게 하고 이 과정을 반복하여 원하는 만큼 필터를 추가할 수 있다.
?CNN의 convolution layer는 그럼 기존의 공간영역의 필터를 가중치 매개변수를 이용하여 학습하게 만드는건가? 맞다 기존에 필터의 값을 직접 설계했다면 이제는 가중치 매개변수의 학습으로 해결하는 것이다.
하지만 이 과정은 내적의 연속이므로 선형연산자만이 사용되고 있어서 단일 컨볼루션 레이어와 동일한 표현력을 갖는다. 그래서 합성곱 레이어 사이에 활성화 함수를 추가해준다. 

필터를 돌리면서 맵이 작아진다는 것이다 하지만 크기를 유지하고 싶다면 패딩을 이용한다. 그냥 겉에 0을 추가 해주면 된다. 왜 0을 붙일까?
?왜 신호처리 쪽에서 문제가 생길까?

receptive fields
단일 컨볼루션에서 각 출력은 입력의 국소 영역이다. 하지만 이것들이 계속 쌓이면 신경망에 의해서 확장되게 된다. 그럼 이걸 어떻게 빠르게 할 수 있을까? strided conv를 하는 것이다 이미지 전체에 필터를 적용하는 대신 일부 영역을 건너뛴다. 그래서 적은 영역으로 크고 효과적인 영역을 구축할 수 있다.

pooling
다운 샘플링 해서 쌓기